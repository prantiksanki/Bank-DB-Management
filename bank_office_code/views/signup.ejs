<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HCL Bank</title>

    <style>
       body {
    font-family: 'Roboto', sans-serif;
    background: linear-gradient(135deg, #ffffffcc, #99efaf);
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    color: #333;
}

.signup-container {
    background-color: #fff;
    padding: 30px 40px;
    border-radius: 12px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    max-width: 400px;
    width: 100%;
    text-align: center;
    animation: fadeIn 1s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

h1 {
    font-size: 24px;
    font-weight: bold;
    color: #4CAF50;
    margin-bottom: 20px;
}

form {
    display: flex;
    flex-direction: column;
    gap: 20px;
}

input[type="text"],
select {
    padding: 12px 15px;
    font-size: 16px;
    border: 1px solid #ddd;
    border-radius: 8px;
    background-color: #f9f9f9;
    transition: all 0.3s ease;
}

input[type="text"]:focus,
select:focus {
    border-color: #4CAF50;
    background-color: #fff;
    box-shadow: 0 0 5px rgba(76, 175, 80, 0.5);
    outline: none;
}

button {
    padding: 12px;
    font-size: 18px;
    font-weight: bold;
    background-color: #4CAF50;
    color: white;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.3s ease;
}

button:hover {
    background-color: #45a049;
    transform: scale(1.05);
}

label {
    font-size: 14px;
    color: #555;
    text-align: left;
}

select {
    cursor: pointer;
}

/* Add subtle hover effect for dropdown options */
option:hover {
    background-color: #f1f1f1;
}

    </style>
</head>
<body>
    <!-- <nav>
        <li>Home</li>
        <li>About</li>
        <li>Contact Us</li>
    </nav> -->
    <form action="/signup" method="POST">
        <input type="text" name="name" placeholder="Name" required>
        <br><br>
        <input type="text" name="phone" placeholder="Phone" required>
        <br><br>

        <input type="text" name="address" placeholder="Address" required>
        <br><br>

        <label for="bankId">Choose bankId:</label>
        <select name="bankId" id="bankId">
            <option value= 12345 >12345 (Kharar)</option>
            <option value=12346>12346 (Ghatal)</option>
            <option value=12347>12347 (Dehradun)</option>
            <option value=12348>12348 (Kota)</option>
        </select>
        <br><br>

        <label for="bankAddress">Choose Address:</label>
        <select name="bankAddress" id="bankAddress">
            <option value="Kharar , Ghatal ,Paschim Medinipur">Kharar,Ghatal,Paschim Medinipur</option>
            <option value="Ghatal , Ghatal , Paschim Medinipur">Ghatal,Ghatal,Paschim Medinipur</option>
            <option value="Dehradun , Dehradun , Uttarakhand">Dehradun,Dehradun,Uttarakhand</option>
            <option value="Kota , Kota , Rajasthan">Kota,Kota,Rajasthan</option>
        </select>

        <br><br>
        <button type="submit">Sign Up</button>
    </form>
    
</body>
</html>